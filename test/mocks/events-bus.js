define(['test/mocks/deferred'], function(Deferred) {

  function EventsBus() {
  }

  EventsBus.prototype.when = function() {
  };

  EventsBus.prototype.then = function() {
  };

  EventsBus.prototype.trigger = function() {
  };

  return EventsBus;
});