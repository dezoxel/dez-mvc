define(['test/mocks/deferred'], function(Deferred) {

  function Router() {
  }

  Router.prototype.start = function() {
  };

  Router.prototype.reset = function() {
  };

  Router.prototype.when = function() {
  };

  Router.prototype.whenNotFound = function() {
  };

  Router.prototype.bind = function() {
  };

  Router.prototype.defaultRoute = function() {
  };

  return Router;
});