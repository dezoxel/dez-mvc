/* global describe, it, expect, beforeEach */
define(['dez-mvc/app'], function(app) {
  'use strict';

  describe('App', function() {
    it('dummy', function() {
      expect(true).toEqual(true);
    });
  });

});